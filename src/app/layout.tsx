import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Fenil Chauhan - Cybersecurity Professional",
  description: "Cybersecurity professional with expertise in ISO 27001, GDPR, and cloud security. Specializing in AI-based security solutions and vulnerability assessments.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
